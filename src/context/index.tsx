// Index file for the context module, it exports all the context providers and hooks.

import { FilterProvider } from './FilterContext';
import { ThemeProvider } from './ThemeContext';

export {
  FilterProvider,
  ThemeProvider,
};